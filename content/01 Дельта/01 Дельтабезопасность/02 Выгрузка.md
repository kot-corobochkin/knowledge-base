- Иерархия
    
    `ReportsController getLocalReportAction`
    
    `LocalReport getLocalReport($data);`
    
    library/Delta/Report/LocalReport.php:506 - определение логики абстрактный
    
    - Если параметры
        
        взять параметры по id library/Delta/Report/Contractor/ContractorLoader.php:3415
        
        получить репорт `$result['rdl'] = $this->getReportBySourceName('rdl', $params);`
        
    
    req_110 - innip ornip fio `getDataByPersonId` `getDataByRequestId`
    
    - Параметр
        
        ```
        Array
        (
            [inn] => 372800602025
            [birth_date] => 1977-12-20
            [custom] => Array
                (
                    [birth_date] => 1977-12-20
                    [alter_name] => Array
                        (
                            [0] => Array
                                (
                                    [last_name] => Чудов
                                    [first_name] => Александр
                                    [patter_name] => Сергеевич
                                )
        
                        )
        
                )
        
        )
        ```
        
    
    `createLocalSourceReport` `makePersonReport`
    

```php
$componentsGroup->load(null, $periodId, true);
$sourceData = $componentsGroup->find($source);
if (!empty($sourceData)) {
    if ($sourceData->isAllowed()) {

$dispatchParams = $this->setDispatchParams(
    'person',
    $personId,
    $allGetValues,
    $summaryReport,
    $source,
    $data['innip'] ?? ''
);

$classToUse = $source;
if (strpos($source, '-')) {
    $nameParts = explode('-', $source);
    foreach ($nameParts as &$namePart) {
        $namePart = ucfirst($namePart);
    }
    $classToUse = implode('', $nameParts);
}

$className = 'Delta\\Report\\Sources\\\\' . ucfirst($classToUse);

if (class_exists($className)) {
    $classObj = new $className();
    $action = $dispatchParams['action'];
    $dispatchParams['query'] = $allGetValues;

    // Вызов метода нужного класса

    $res = $classObj->$action($dispatchParams);
    echo "sergey <pre>".print_r($dispatchParams,true)."</pre>";exit;//todo: remove
}
```

внешние источники library/Delta/Office/Doc/ExternalSourcesReport.php:19180

локальные источники library/Delta/Office/Doc/DocReport.php:60

`Sources\\FinancialInf`

library/Delta/Report/LocalReport.php:1032 - Здесь вызов класса например класса аналитики