 sudo -u apache php index.php monitor list --print_r | grep -n -A 40 -B 80 "ParserApiProcessor::checkBalance"
 Вывести принт и показать строки до и после

убить запрос
exec("pkill -f 'monitor start-sheduler'");

 ps -eo pid,stat | awk '$2 ~ /^T/ {print $1}' | xargs kill -CONT
 убить процессы какие висят
 
 <details><summary>дебаг бактрейс</summary>echo "=== DEBUG BACKTRACE ===";  
$trace = debug_backtrace(DEBUG_BACKTRACE_IGNORE_ARGS);  
foreach($trace as $index => $call) {  
    echo "\n" . $index . ": " .  
        (isset($call['file']) ? basename($call['file']) : 'unknown') .  
        ":" . (isset($call['line']) ? $call['line'] : 'unknown') .  
        " -> " . (isset($call['function']) ? $call['function'] : 'unknown') .  
        (isset($call['class']) ? " (" . $call['class'] . ")" : "");  
}  
echo "\n===================";</details>


